---
layout: base.njk
title: المنتجات
---

<section class="hero d-flex align-items-center justify-content-center text-center" style="height: 400px; background: linear-gradient(135deg, var(--primary-wood) 0%, var(--primary-wood-light) 100%); color: white;">
  <div class="container">
    <h1 class="display-3 fw-bold mb-3">منتجاتنا</h1>
    <p class="lead text-light">اكتشف تشكيلة واسعة من الأثاث الفاخر</p>
  </div>
</section>

<section class="py-5">
  <div class="container">
    <h2 class="text-center mb-5 section-title">تصفح حسب الفئة</h2>

    <div class="d-flex flex-wrap justify-content-center gap-2 mb-5">
      <button class="btn btn-outline-dark filter-btn active" data-filter="all">
        <i class="fas fa-th me-1"></i> جميع المنتجات
      </button>
      <button class="btn btn-outline-dark filter-btn" data-filter="bedrooms">
        <i class="fas fa-bed me-1"></i> غرف نوم رئيسية
      </button>
      <button class="btn btn-outline-dark filter-btn" data-filter="kids-bedrooms">
        <i class="fas fa-child me-1"></i> غرف نوم أطفال
      </button>
      <button class="btn btn-outline-dark filter-btn" data-filter="sofas">
        <i class="fas fa-couch me-1"></i> أنتريهات
      </button>
      <button class="btn btn-outline-dark filter-btn" data-filter="corners">
        <i class="fas fa-chair me-1"></i> ركنات
      </button>
      <button class="btn btn-outline-dark filter-btn" data-filter="dining">
        <i class="fas fa-utensils me-1"></i> غرف سفرة
      </button>
      <button class="btn btn-outline-dark filter-btn" data-filter="kitchens">
        <i class="fas fa-blender me-1"></i> مطابخ
      </button>
    </div>

    <!-- حاوية المنتجات الديناميكية -->
    <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4" id="products-container">
      {% for product in products.products %}
      <div class="col product-card" data-category="{{ product.category }}">
        <div class="card h-100 shadow-sm border-0 position-relative overflow-hidden group-hover-effect">
            <div class="position-relative" style="height: 250px; overflow: hidden;">
                <img src="/{{ product.image }}" class="card-img-top w-100 h-100 object-fit-cover transition-transform" alt="{{ product.name }}" onclick="window.location.href='/product/{{ product.id | slug }}/'" style="cursor: pointer;">
                <span class="position-absolute top-0 end-0 m-2 badge bg-warning text-dark fs-6 shadow-sm">{{ product.price }} ج.م</span>
            </div>
          
          <div class="card-body text-center d-flex flex-column">
            <h5 class="card-title fw-bold text-primary mb-2">
                <a href="/product/{{ product.id | slug }}/" class="text-decoration-none text-dark stretched-link">{{ product.name }}</a>
            </h5>
            <p class="card-text text-muted small flex-grow-1">{{ product.description }}</p>
            
            <div class="d-flex justify-content-center gap-1 flex-wrap mb-3">
             {% for feature in product.features %}
                <span class="badge bg-light text-dark border">{{ feature }}</span>
             {% endfor %}
            </div>

            <button class="btn btn-primary w-100 mt-auto position-relative z-2" onclick="event.stopPropagation(); addToCart('{{ product.id }}')">
              <i class="fas fa-cart-plus me-1"></i> أضف للسلة
            </button>
          </div>
        </div>
      </div>
      {% endfor %}
    </div>
  </div>
</section>

<section class="py-5 text-white" style="background: linear-gradient(135deg, var(--primary-wood) 0%, var(--primary-wood-light) 100%);">
  <div class="container">
    <h2 class="text-center mb-5 text-white position-relative d-inline-block w-100">لماذا تختار منتجاتنا؟</h2>

    <div class="row g-4 text-center">
      <div class="col-md-4">
        <div class="p-4 rounded-3 h-100" style="background: rgba(255, 255, 255, 0.1);">
          <div class="mb-3 text-warning display-5">
            <i class="fas fa-check-double"></i>
          </div>
          <h3 class="h4 text-white">جودة مضمونة</h3>
          <p class="text-light opacity-75">جميع منتجاتنا مصنوعة من أجود الخامات</p>
        </div>
      </div>

      <div class="col-md-4">
        <div class="p-4 rounded-3 h-100" style="background: rgba(255, 255, 255, 0.1);">
          <div class="mb-3 text-warning display-5">
            <i class="fas fa-palette"></i>
          </div>
          <h3 class="h4 text-white">تصاميم متنوعة</h3>
          <p class="text-light opacity-75">تشكيلة واسعة لتناسب جميع الأذواق</p>
        </div>
      </div>

      <div class="col-md-4">
        <div class="p-4 rounded-3 h-100" style="background: rgba(255, 255, 255, 0.1);">
          <div class="mb-3 text-warning display-5">
            <i class="fas fa-money-bill-wave"></i>
          </div>
          <h3 class="h4 text-white">أسعار تنافسية</h3>
          <p class="text-light opacity-75">أفضل قيمة مقابل المال</p>
        </div>
      </div>
    </div>

    <div class="text-center mt-5">
      <a href="/offers/" class="btn btn-warning btn-lg mx-2 text-dark fw-bold">
        <i class="fas fa-tags me-1"></i> العروض الخاصة
      </a>
      <a href="/contact/" class="btn btn-outline-light btn-lg mx-2">
        <i class="fas fa-phone me-1"></i> تواصل معنا
      </a>
    </div>
  </div>
</section>
