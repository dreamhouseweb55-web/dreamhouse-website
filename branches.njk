---
layout: base.njk
title: {{ branches_page.hero.title }}
---

<!-- Hero Section -->
<section class="d-flex align-items-center justify-content-center text-center text-white" style="height: 400px; background: {% if branches_page.hero.image %}linear-gradient(rgba(0,0,0,0.5), rgba(0,0,0,0.5)), url('{{ branches_page.hero.image }}') no-repeat center center/cover{% else %}linear-gradient(135deg, var(--primary-wood) 0%, var(--primary-wood-light) 100%){% endif %};">
  <div class="container">
    <h1 class="display-3 fw-bold mb-3">{{ branches_page.hero.title }}</h1>
    <p class="lead fs-3">{{ branches_page.hero.subtitle }}</p>
  </div>
</section>

<!-- Branches Grid -->
<section class="py-5">
  <div class="container">
    <div class="text-center mb-5">
      <h2 class="fw-bold section-title">فروع دريم هاوس</h2>
      <p class="text-muted">زرنا في أي من فروعنا وتمتع بتجربة تسوق فريدة</p>
    </div>

    <div class="row g-4 justify-content-center">
      {% for branch in branches_page.branches %}
      <div class="col-lg-4 col-md-6">
        <div class="card h-100 border-0 shadow-sm overflow-hidden hover-lift transition-all rounded-4">
          <!-- Map Container -->
          <div class="position-relative" style="height: 300px;">
            {% if branch.mapEmbed %}
            <iframe 
              src="{{ branch.mapEmbed }}" 
              width="100%" 
              height="100%" 
              style="border:0;" 
              allowfullscreen="" 
              loading="lazy" 
              class="w-100 h-100"
              referrerpolicy="no-referrer-when-downgrade">
            </iframe>
            {% else %}
            <!-- Fallback if no map -->
            <div class="w-100 h-100 d-flex align-items-center justify-content-center bg-light text-muted">
                <i class="fas fa-map-marked-alt fa-3x"></i>
            </div>
            {% endif %}
          </div>
          
          <div class="card-body p-4 text-center d-flex flex-column">
            <h3 class="h4 fw-bold mb-3 text-dark">{{ branch.name }}</h3>
            
            <div class="mb-4">
                <p class="text-muted small mb-2 d-flex align-items-center justify-content-center gap-2">
                    <i class="fas fa-map-marker-alt text-warning"></i>
                    {{ branch.address }}
                </p>
                {% if branch.phone %}
                <p class="text-muted small mb-0 d-flex align-items-center justify-content-center gap-2">
                    <i class="fas fa-phone text-warning"></i>
                    {{ branch.phone }}
                </p>
                {% endif %}
            </div>
            
            <div class="mt-auto d-flex gap-2 justify-content-center">
              {% if branch.mapLink %}
              <a href="{{ branch.mapLink }}" target="_blank" class="btn btn-primary rounded-pill px-4 flex-grow-1">
                <i class="fas fa-location-arrow me-2"></i> اللوكيشن
              </a>
              {% endif %}
              <a href="tel:{{ branch.phone }}" class="btn btn-outline-dark rounded-circle" style="width: 40px; height: 40px; padding: 0; display: flex; align-items: center; justify-content: center;">
                <i class="fas fa-phone"></i>
              </a>
            </div>
          </div>
        </div>
      </div>
      {% endfor %}
    </div>
  </div>
</section>

<!-- Reach Us Section -->
<section class="py-5 bg-light">
    <div class="container">
        <h2 class="text-center mb-5 fw-bold section-title">{{ branches_page.reach_us.title }}</h2>
        <div class="row g-4 justify-content-center">
            {% for method in branches_page.reach_us.methods %}
            <div class="col-md-6 col-lg-5">
                <div class="card h-100 border-0 shadow-sm">
                    <div class="card-body p-4">
                        <h3 class="h5 fw-bold mb-3 d-flex align-items-center">
                            <i class="{{ method.icon }} text-primary me-2"></i> {{ method.title }}
                        </h3>
                        <p class="text-muted mb-3">{{ method.description }}</p>
                        {% if method.note %}
                        <div class="alert alert-secondary d-flex align-items-center mb-0 py-2">
                             <i class="fas fa-info-circle text-warning me-2"></i> <strong>{{ method.note }}</strong>
                        </div>
                        {% endif %}
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>
    </div>
</section>

<!-- Call to Action -->
<section class="py-5 text-center text-white" style="background: linear-gradient(135deg, var(--primary-wood) 0%, var(--primary-wood-light) 100%);">
  <div class="container">
    <h2 class="fw-bold mb-3">جاهز لفرش بيتك؟</h2>
    <p class="lead text-white-50 mb-4">
      تواصل معنا الآن أو زر أقرب فرع ليك للاستفادة من عروضنا الحصرية
    </p>

    <div class="d-flex justify-content-center gap-3 flex-wrap">
      <a href="https://wa.me/{{ settings.contact.whatsapp }}" class="btn btn-warning fw-bold text-dark btn-lg" target="_blank">
        <i class="fab fa-whatsapp me-2"></i> تواصل واتساب
      </a>
      <a href="tel:{{ settings.contact.phone }}" class="btn btn-outline-light btn-lg">
        <i class="fas fa-phone me-2"></i> اتصل بنا
      </a>
    </div>
  </div>
</section>
