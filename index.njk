---
layout: base.njk
title: الرئيسية
---
  <!-- ===== القسم الرئيسي (Hero) ===== -->
  <section class="d-flex align-items-center justify-content-center text-center position-relative text-white" style="min-height: 80vh; background: linear-gradient(rgba(0,0,0,0.5), rgba(0,0,0,0.5)), url('{{ home.hero.image }}') no-repeat center center/cover; background-color: var(--primary-wood);">
    <div class="container position-relative z-2">
      <h1 class="display-2 fw-bold mb-4" style="text-shadow: 2px 2px 4px rgba(0,0,0,0.5);">{{ home.hero.title }}</h1>
      <p class="lead mb-5 fs-3" style="text-shadow: 1px 1px 2px rgba(0,0,0,0.5);">{{ home.hero.subtitle }}</p>
      <div class="d-flex justify-content-center gap-3 flex-wrap">
        <a href="{{ home.hero.cta.primary.link }}" class="btn btn-warning btn-lg px-5 py-3 fw-bold text-dark">{{ home.hero.cta.primary.text }}</a>
        <a href="{{ home.hero.cta.secondary.link }}" class="btn btn-outline-light btn-lg px-5 py-3 fw-bold">{{ home.hero.cta.secondary.text }}</a>
      </div>
    </div>
  </section>

  <!-- ===== قسم المميزات ===== -->
  <section class="py-5" id="features">
    <div class="container text-center">
      <h2 class="mb-3 fw-bold">{{ home.features.sectionTitle }}</h2>
      <p class="text-muted mb-5">{{ home.features.sectionSubtitle }}</p>
      
      <div class="row g-4">
      {% for feature in home.features.items %}
        <div class="col-md-4">
          <div class="card h-100 border-0 shadow-sm p-4 hover-lift">
            <div class="mb-3 text-warning display-5">
              <i class="{{ feature.icon }}"></i>
            </div>
            <h3 class="h4 fw-bold mb-3">{{ feature.title }}</h3>
            <p class="text-muted mb-0">{{ feature.description }}</p>
          </div>
        </div>
      {% endfor %}
      </div>
    </div>
  </section>

  <!-- ===== قسم المنتجات ===== -->
  <section class="py-5 bg-light" id="products">
    <div class="container">
      <div class="text-center mb-5">
        <h2 class="fw-bold">منتجاتنا المميزة</h2>
        <p class="text-muted">تشكيلة واسعة من الأثاث لجميع الأذواق والمساحات</p>
      </div>
      
      <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
        <!-- Bedrooms -->
        <div class="col">
            <div class="card h-100 border-0 shadow overflow-hidden group-hover-effect">
                <div class="product-image d-flex align-items-center justify-content-center text-white" style="height: 250px; background: linear-gradient(135deg, #8B7355 0%, #D4AF37 100%);">
                    <i class="fas fa-bed display-1"></i>
                </div>
                <div class="card-body text-center d-flex flex-column">
                    <h3 class="h5 fw-bold mb-2">غرف نوم رئيسية</h3>
                    <p class="card-text text-muted small mb-3">تصاميم عصرية وكلاسيكية لغرف نوم فاخرة بأحجام مختلفة</p>
                    <div class="d-flex justify-content-center gap-2 mb-3">
                        <span class="badge bg-light text-dark border">خشب طبيعي</span>
                        <span class="badge bg-light text-dark border">ضمان 5 سنوات</span>
                    </div>
                    <a href="/products/#bedrooms" class="btn btn-outline-primary w-100 mt-auto stretched-link">اعرف المزيد</a>
                </div>
            </div>
        </div>

        <!-- Kids Bedrooms -->
        <div class="col">
            <div class="card h-100 border-0 shadow overflow-hidden group-hover-effect">
                <div class="product-image d-flex align-items-center justify-content-center text-white" style="height: 250px; background: linear-gradient(135deg, #FF6B9D 0%, #C44569 100%);">
                    <i class="fas fa-child display-1"></i>
                </div>
                <div class="card-body text-center d-flex flex-column">
                    <h3 class="h5 fw-bold mb-2">غرف نوم أطفال</h3>
                    <p class="card-text text-muted small mb-3">غرف نوم ملونة وآمنة لأطفالك بتصاميم مبتكرة</p>
                     <div class="d-flex justify-content-center gap-2 mb-3">
                        <span class="badge bg-light text-dark border">ألوان زاهية</span>
                        <span class="badge bg-light text-dark border">آمن للأطفال</span>
                    </div>
                    <a href="/products/#kids-bedrooms" class="btn btn-outline-primary w-100 mt-auto stretched-link">اعرف المزيد</a>
                </div>
            </div>
        </div>

        <!-- Sofas -->
        <div class="col">
            <div class="card h-100 border-0 shadow overflow-hidden group-hover-effect">
                <div class="product-image d-flex align-items-center justify-content-center text-white" style="height: 250px; background: linear-gradient(135deg, #5F4B8B 0%, #A393BF 100%);">
                    <i class="fas fa-couch display-1"></i>
                </div>
                <div class="card-body text-center d-flex flex-column">
                    <h3 class="h5 fw-bold mb-2">أنتريهات</h3>
                    <p class="card-text text-muted small mb-3">أنتريهات أمريكان ومودرن بأشكال متنوعة وأحجام مختلفة</p>
                     <div class="d-flex justify-content-center gap-2 mb-3">
                        <span class="badge bg-light text-dark border">قماش فاخر</span>
                        <span class="badge bg-light text-dark border">إسفنج عالي الكثافة</span>
                    </div>
                    <a href="/products/#sofas" class="btn btn-outline-primary w-100 mt-auto stretched-link">اعرف المزيد</a>
                </div>
            </div>
        </div>

        <!-- Corners -->
        <div class="col">
            <div class="card h-100 border-0 shadow overflow-hidden group-hover-effect">
                <div class="product-image d-flex align-items-center justify-content-center text-white" style="height: 250px; background: linear-gradient(135deg, #E67E22 0%, #D68910 100%);">
                    <i class="fas fa-chair display-1"></i>
                </div>
                <div class="card-body text-center d-flex flex-column">
                    <h3 class="h5 fw-bold mb-2">ركنات</h3>
                    <p class="card-text text-muted small mb-3">ركنات عصرية بأشكال وأحجام تناسب جميع المساحات</p>
                     <div class="d-flex justify-content-center gap-2 mb-3">
                        <span class="badge bg-light text-dark border">تصاميم حديثة</span>
                        <span class="badge bg-light text-dark border">راحة فائقة</span>
                    </div>
                    <a href="/products/#corners" class="btn btn-outline-primary w-100 mt-auto stretched-link">اعرف المزيد</a>
                </div>
            </div>
        </div>

        <!-- Dining -->
        <div class="col">
            <div class="card h-100 border-0 shadow overflow-hidden group-hover-effect">
                <div class="product-image d-flex align-items-center justify-content-center text-white" style="height: 250px; background: linear-gradient(135deg, #27AE60 0%, #229954 100%);">
                    <i class="fas fa-utensils display-1"></i>
                </div>
                <div class="card-body text-center d-flex flex-column">
                    <h3 class="h5 fw-bold mb-2">غرف سفرة</h3>
                    <p class="card-text text-muted small mb-3">غرف سفرة كاملة بتصاميم أنيقة لضيافة مميزة</p>
                     <div class="d-flex justify-content-center gap-2 mb-3">
                        <span class="badge bg-light text-dark border">خشب فاخر</span>
                        <span class="badge bg-light text-dark border">تشطيب راقي</span>
                    </div>
                    <a href="/products/#dining" class="btn btn-outline-primary w-100 mt-auto stretched-link">اعرف المزيد</a>
                </div>
            </div>
        </div>

        <!-- Kitchens -->
        <div class="col">
              <div class="card h-100 border-0 shadow overflow-hidden group-hover-effect">
                <div class="product-image d-flex align-items-center justify-content-center text-white" style="height: 250px; background: linear-gradient(135deg, #2C3E50 0%, #34495E 100%);">
                    <i class="fas fa-blender display-1"></i>
                </div>
                <div class="card-body text-center d-flex flex-column">
                    <h3 class="h5 fw-bold mb-2">مطابخ</h3>
                    <p class="card-text text-muted small mb-3">مطابخ عصرية مع خدمة تصميم مجانية في الفرع الجديد</p>
                     <div class="d-flex justify-content-center gap-2 mb-3">
                        <span class="badge bg-info text-white border border-info">قريباً</span>
                        <span class="badge bg-light text-dark border">تصميم مجاني</span>
                    </div>
                    <a href="/products/#kitchens" class="btn btn-outline-primary w-100 mt-auto stretched-link">اعرف المزيد</a>
                </div>
            </div>
        </div>

      </div>
    </div>
  </section>

  <!-- ===== قسم الجودة (الفيل) ===== -->
  <section class="py-5 position-relative overflow-hidden">
    <div class="container">
      <div class="row align-items-center bg-white rounded-4 shadow-sm overflow-hidden border border-light">
        <div class="col-lg-6 p-0 order-lg-2">
           <img src="{{ home.quality.image }}" alt="{{ home.quality.title }}" class="img-fluid w-100">
        </div>
        <div class="col-lg-6 p-5">
          <div class="text-start">
            <span class="badge bg-warning text-dark px-3 py-2 mb-3 fw-bold rounded-pill">{{ home.quality.badge }}</span>
            <h2 class="display-5 fw-bold mb-4 text-primary">{{ home.quality.title }}</h2>
            <p class="lead text-muted mb-4 text-dark fs-3 font-monospace">{{ home.quality.subtitle }}</p>
            <p class="text-muted mb-5">
              {{ home.quality.description }}
            </p>
            <a href="{{ home.quality.buttonLink }}" class="btn btn-outline-primary rounded-pill px-4">
              <i class="fas fa-shield-alt me-2"></i> {{ home.quality.buttonText }}
            </a>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ===== قسم الأسئلة الشائعة ===== -->
  <section class="py-5 bg-light" id="faq">
    <div class="container">
      <div class="text-center mb-5">
        <h2 class="fw-bold mb-3">{{ home.faq.title }}</h2>
        <p class="text-muted">{{ home.faq.subtitle }}</p>
      </div>
      
      <div class="row justify-content-center">
        <div class="col-lg-8">
          <div class="accordion" id="accordionFAQ">
            {% for item in home.faq.items %}
            <div class="accordion-item border-0 shadow-sm mb-3 rounded overflow-hidden">
              <h2 class="accordion-header" id="heading{{ loop.index }}">
                <button class="accordion-button {% if not loop.first %}collapsed{% endif %} fw-bold bg-white" type="button" data-bs-toggle="collapse" data-bs-target="#collapse{{ loop.index }}" aria-expanded="{% if loop.first %}true{% else %}false{% endif %}" aria-controls="collapse{{ loop.index }}">
                  {{ item.question }}
                </button>
              </h2>
              <div id="collapse{{ loop.index }}" class="accordion-collapse collapse {% if loop.first %}show{% endif %}" aria-labelledby="heading{{ loop.index }}" data-bs-parent="#accordionFAQ">
                <div class="accordion-body bg-white text-muted">
                  {{ item.answer }}
                </div>
              </div>
            </div>
            {% endfor %}
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ===== قسم الإحصائيات ===== -->
  <section class="py-5 text-white" id="stats" style="background: linear-gradient(135deg, var(--primary-wood) 0%, var(--primary-wood-light) 100%);">
    <div class="container text-center">
      <h2 class="mb-5 d-inline-block border-bottom pb-2 border-warning">{{ home.stats.sectionTitle }}</h2>
      <div class="row g-4">
      {% for stat in home.stats.items %}
        <div class="col-md-3 col-6">
          <div class="p-4 rounded bg-white bg-opacity-10 border border-warning h-100">
            <div class="mb-3 text-warning display-5">
              <i class="{{ stat.icon }}"></i>
            </div>
            <div class="display-6 fw-bold mb-2"><span class="counter" data-target="{{ stat.number }}">0</span>+</div>
            <p class="mb-0 text-white-50">{{ stat.label }}</p>
          </div>
        </div>
      {% endfor %}
      </div>
    </div>
  </section>

  <!-- ===== قسم الفروع ===== -->
  <section class="py-5 bg-light" id="branches">
    <div class="container">
      <div class="text-center mb-5">
        <h2 class="fw-bold mb-3">زورنا في فروعنا</h2>
        <p class="text-muted">زيارة أي من فروعنا لتجربة تسوق فريدة ومشاهدة المنتجات مباشرة</p>
      </div>
      
      <div class="row g-4 justify-content-center">
        {% for branch in branches_page.branches %}
        <div class="col-lg-4 col-md-6">
          <div class="card h-100 border-0 shadow-sm overflow-hidden hover-lift transition-all rounded-4">
            <!-- Map Container -->
            <div class="position-relative" style="height: 300px;">
              {% if branch.mapEmbed %}
              <iframe 
                src="{{ branch.mapEmbed }}" 
                width="100%" 
                height="100%" 
                style="border:0;" 
                allowfullscreen="" 
                loading="lazy" 
                class="w-100 h-100"
                referrerpolicy="no-referrer-when-downgrade">
              </iframe>
              {% else %}
              <!-- Fallback if no map -->
              <div class="w-100 h-100 d-flex align-items-center justify-content-center bg-light text-muted">
                  <i class="fas fa-map-marked-alt fa-3x"></i>
              </div>
              {% endif %}
            </div>
            
            <div class="card-body p-4 text-center d-flex flex-column">
              <h3 class="h4 fw-bold mb-3 text-dark">{{ branch.name }}</h3>
              
              <div class="mb-4">
                  <p class="text-muted small mb-2 d-flex align-items-center justify-content-center gap-2">
                      <i class="fas fa-map-marker-alt text-warning"></i>
                      {{ branch.address }}
                  </p>
                  {% if branch.phone %}
                  <p class="text-muted small mb-0 d-flex align-items-center justify-content-center gap-2">
                      <i class="fas fa-phone text-warning"></i>
                      {{ branch.phone }}
                  </p>
                  {% endif %}
              </div>
              
              <div class="mt-auto d-flex gap-2 justify-content-center">
                {% if branch.mapLink %}
                <a href="{{ branch.mapLink }}" target="_blank" class="btn btn-primary rounded-pill px-4 flex-grow-1">
                  <i class="fas fa-location-arrow me-2"></i> اللوكيشن
                </a>
                {% endif %}
                <a href="tel:{{ branch.phone }}" class="btn btn-outline-dark rounded-circle" style="width: 40px; height: 40px; padding: 0; display: flex; align-items: center; justify-content: center;">
                  <i class="fas fa-phone"></i>
                </a>
              </div>
            </div>
          </div>
        </div>
        {% endfor %}
      </div>
    </div>
  </section>

  <!-- ===== قسم الاتصال السريع ===== -->
  <section class="py-5 bg-light" id="quick-contact">
    <div class="container text-center">
      <h2 class="fw-bold mb-3">تواصل معنا الآن</h2>
      <p class="text-muted mb-4">فريقنا جاهز لمساعدتك في اختيار الأثاث المناسب</p>
      
      <div class="d-flex justify-content-center gap-3 flex-wrap">
        <a href="/contact/" class="btn btn-primary btn-lg">
          <i class="fas fa-envelope me-2"></i> أرسل استفسار
        </a>
        <a href="https://wa.me/{{ settings.contact.whatsapp }}" class="btn btn-warning btn-lg text-dark fw-bold" target="_blank">
          <i class="fab fa-whatsapp me-2"></i> واتساب
        </a>
        <a href="tel:{{ settings.contact.phone }}" class="btn btn-outline-dark btn-lg">
          <i class="fas fa-phone me-2"></i> اتصل بنا
        </a>
      </div>
    </div>
  </section>
