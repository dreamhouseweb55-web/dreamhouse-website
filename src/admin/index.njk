---
layout: null
permalink: "/admin/index.html"
---
<!DOCTYPE html>
<html dir="rtl" lang="ar">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>لوحة التحكم - Dream House</title>
    <style>
        * { box-sizing: border-box; margin: 0; padding: 0; }
        body { font-family: 'Segoe UI', Tahoma, sans-serif; background: #f5f5f5; }
        
        .login-page {
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        }
        .login-box {
            background: white;
            padding: 40px;
            border-radius: 16px;
            box-shadow: 0 20px 60px rgba(0,0,0,0.3);
            text-align: center;
            width: 90%;
            max-width: 400px;
        }
        .login-logo { width: 120px; margin-bottom: 20px; }
        .login-title { color: #333; margin-bottom: 10px; font-size: 24px; }
        .login-subtitle { color: #666; margin-bottom: 30px; font-size: 14px; }
        .login-input {
            width: 100%;
            padding: 15px;
            border: 2px solid #e0e0e0;
            border-radius: 10px;
            font-size: 16px;
            text-align: center;
            margin-bottom: 15px;
        }
        .login-input:focus { outline: none; border-color: #667eea; }
        .login-btn {
            width: 100%;
            padding: 15px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            border-radius: 10px;
            font-size: 16px;
            font-weight: bold;
            cursor: pointer;
        }
        .login-btn:hover { opacity: 0.9; }
        .login-error {
            color: #dc3545;
            margin-top: 15px;
            display: none;
            padding: 10px;
            background: #f8d7da;
            border-radius: 8px;
        }
        .login-error.show { display: block; }
        
        #cms-container { display: none; }
        #cms-container.show { display: block; }
        
        .loading {
            text-align: center;
            padding: 100px 20px;
            font-size: 18px;
            color: #666;
        }
        .loading .spinner {
            width: 50px;
            height: 50px;
            border: 4px solid #f3f3f3;
            border-top: 4px solid #667eea;
            border-radius: 50%;
            animation: spin 1s linear infinite;
            margin: 0 auto 20px;
        }
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
    </style>
</head>
<body>
    <!-- Login Page -->
    <div id="login-page" class="login-page">
        <div class="login-box">
            <img src="/images/logo-dark.webp" alt="Dream House" class="login-logo" onerror="this.style.display='none'">
            <h1 class="login-title">لوحة التحكم</h1>
            <p class="login-subtitle">أدخل كلمة المرور للوصول</p>
            
            <form id="login-form">
                <input type="password" id="password" class="login-input" placeholder="كلمة المرور" required>
                <button type="submit" class="login-btn">🔓 دخول</button>
            </form>
            
            <div id="login-error" class="login-error">❌ كلمة المرور غير صحيحة</div>
        </div>
    </div>

    <!-- CMS Container -->
    <div id="cms-container">
        <div class="loading" id="loading">
            <div class="spinner"></div>
            <p>جاري تحميل لوحة التحكم...</p>
        </div>
    </div>

    <script>
    (function() {
        // === CONFIG ===
        var VALID_PASSWORD = 'dreamhouse1452';
        var SESSION_KEY = 'dh_admin_auth';
        var SESSION_DURATION = 24 * 60 * 60 * 1000;
        
        function checkSession() {
            var session = localStorage.getItem(SESSION_KEY);
            if (session) {
                try {
                    var data = JSON.parse(session);
                    if (data.valid && Date.now() < data.expires) return true;
                } catch(e) {}
            }
            return false;
        }
        
        function saveSession() {
            localStorage.setItem(SESSION_KEY, JSON.stringify({
                valid: true,
                expires: Date.now() + SESSION_DURATION
            }));
        }
        
        function loadCMS() {
            document.getElementById('login-page').style.display = 'none';
            document.getElementById('cms-container').className = 'show';
            
            var script = document.createElement('script');
            script.src = 'https://unpkg.com/decap-cms@^3.0.0/dist/decap-cms.js';
            script.onload = function() {
                document.getElementById('loading').style.display = 'none';
            };
            document.body.appendChild(script);
        }
        
        if (checkSession()) loadCMS();
        
        document.getElementById('login-form').addEventListener('submit', function(e) {
            e.preventDefault();
            var password = document.getElementById('password').value;
            
            if (password === VALID_PASSWORD) {
                saveSession();
                loadCMS();
            } else {
                document.getElementById('login-error').className = 'login-error show';
                document.getElementById('password').value = '';
            }
        });
    })();
    </script>
</body>
</html>
