---
layout: base.njk
title: من نحن
---
  <section class="d-flex align-items-center justify-content-center text-center text-white" style="height: 400px; background: {% if about.hero.image %}linear-gradient(rgba(0,0,0,0.5), rgba(0,0,0,0.5)), url('{{ about.hero.image }}') no-repeat center center/cover{% else %}linear-gradient(135deg, var(--primary-wood) 0%, var(--primary-wood-light) 100%){% endif %};">
    <div class="container">
      <h1 class="display-3 fw-bold mb-3">{{ about.hero.title }}</h1>
      <p class="lead fs-3">{{ about.hero.subtitle }}</p>
    </div>
  </section>

  <section class="py-5">
    <div class="container">
      <h2 class="text-center mb-4 fw-bold section-title">{{ about.intro.title }}</h2>

      <div class="row justify-content-center mb-5">
        <div class="col-lg-8 text-center">
            <p class="lead text-muted lh-lg">
              {{ about.intro.description }}
            </p>
        </div>
      </div>

      <div class="row g-4 text-center">
      {% for item in about.visionValues %}
        <div class="col-md-4">
          <div class="card h-100 border-0 shadow-sm p-4">
            <div class="text-danger display-4 mb-3">
              <i class="{{ item.icon }}"></i>
            </div>
            <h3 class="h4 fw-bold mb-3">{{ item.title }}</h3>
            <p class="text-muted mb-0">{{ item.description }}</p>
          </div>
        </div>
      {% endfor %}
      </div>
    </div>
  </section>

  <section class="py-5 bg-light">
    <div class="container">
      <h2 class="text-center mb-5 fw-bold section-title">{{ about.whyUs.title }}</h2>

      <div class="row g-4">
      {% for item in about.whyUs.items %}
        <div class="col-md-4">
          <div class="card h-100 border-0 shadow-sm p-4">
            <h3 class="h5 fw-bold mb-3 d-flex align-items-center">
              <i class="{{ item.icon }} text-danger me-2 fs-3"></i>
              {{ item.title }}
            </h3>
            <p class="text-muted mb-0 ps-4 border-end border-3 border-danger pe-3">{{ item.description }}</p>
          </div>
        </div>
      {% endfor %}
      </div>
    </div>
  </section>

  <section class="py-5">
    <div class="container">
      <h2 class="text-center mb-5 fw-bold section-title">{{ about.timeline.title }}</h2>

      <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="list-group">
            {% for item in about.timeline.items %}
                <div class="list-group-item list-group-item-action p-4 border-0 shadow-sm mb-3 rounded-3 {% if loop.last %}border-start border-5 border-danger{% endif %}">
                    <div class="d-flex w-100 justify-content-between align-items-center mb-2">
                    <h5 class="mb-1 fw-bold text-primary">
                        <i class="fas fa-calendar-alt me-2 text-muted"></i> {{ item.title }}
                    </h5>
                    <span class="badge bg-danger fs-6">{{ item.year }}</span>
                    </div>
                    <p class="mb-1 text-muted">{{ item.description }}</p>
                </div>
            {% endfor %}
            </div>
        </div>
      </div>
    </div>
  </section>

  <section class="py-5 text-white" style="background: linear-gradient(135deg, var(--primary-wood) 0%, var(--primary-wood-light) 100%);">
    <div class="container">
      <h2 class="text-center mb-3">{{ about.team.title }}</h2>
      <p class="text-center mb-5 text-white-50">
        {{ about.team.subtitle }}
      </p>

      <div class="row g-4 justify-content-center">
      {% for item in about.team.items %}
        <div class="col-md-4">
          <div class="p-4 rounded-3 h-100 text-center" style="background: rgba(255, 255, 255, 0.1);">
            <div class="text-danger display-5 mb-3">
              <i class="{{ item.icon }}"></i>
            </div>
            <h3 class="h4 fw-bold text-white mb-2">{{ item.title }}</h3>
            <p class="text-white-50 mb-0">{{ item.description }}</p>
          </div>
        </div>
      {% endfor %}
      </div>
    </div>
  </section>

  <section class="py-5">
    <div class="container text-center">
      <h2 class="fw-bold mb-3">انضم لعائلة دريم هاوس</h2>
      <p class="text-muted mb-5">ابدأ رحلتك معنا اليوم</p>

      <div class="d-flex justify-content-center gap-3">
        <a href="/products/" class="btn btn-primary btn-lg">
          <i class="fas fa-shopping-cart me-2"></i> تصفح المنتجات
        </a>
        <a href="/contact/" class="btn btn-danger btn-lg fw-bold">
          <i class="fas fa-phone me-2"></i> تواصل معنا
        </a>
      </div>
    </div>
  </section>
